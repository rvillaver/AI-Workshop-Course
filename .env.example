# Copy to .env and fill in ONE provider. .env is gitignored.

# --- OpenRouter (free, recommended) ---  get a key at https://openrouter.ai/keys
OPENROUTER_API_KEY=sk-or-...
# optional overrides:
# OX_MODEL=z-ai/glm-5.2:free
# OX_BASE_URL=https://openrouter.ai/api/v1

# --- Ox Alpha via Tokenra (needs account quota) ---
# OX_FREE_API_TOKEN=sk-...
